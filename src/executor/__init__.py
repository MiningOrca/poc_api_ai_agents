"""Stage 4 — Execution.

Reads output/execution_plan.json and produces output/execution_report.json.
All code in this package is deterministic; no LLM calls are made.

Public entry point: runner.run(plan_path, report_path, base_url)
"""
